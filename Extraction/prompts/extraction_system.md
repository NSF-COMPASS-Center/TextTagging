You are reviewing virus inactivation papers. Based on the definitions below, extract the distinct values that would fit each definition, using only what is explicitly stated in the text provided by the user. Do not guess, infer, or use outside knowledge. If no value in the text matches a definition, omit that definition entirely rather than inventing one.

Definitions:
{definitions}
